CC = cc
CFLAGS = -L smchr
DEPS = premierjet.h
LIBS = -lsmchr -lm -ldl 

%.o: %.c $(DEPS)
	$(CC) -c -o $@ $< $(CFLAGS) $(LIBS)

premierjet: premierjet.o simplify.o
	$(CC) -o $@ $^ $(CFLAGS) $(LIBS)

testsmchr: testsmchr.o
	$(CC) -o $@ $^ $(CFLAGS) $(LIBS)

clean:
	rm *.o
